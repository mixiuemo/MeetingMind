# 声纹与说话人区分 TODO

本文档记录 MeetingMind 声纹功能的实施方案。目标是在不破坏现有 6 秒积木实时转写体验的前提下，识别会议中的说话人切换，并逐步支持实名声纹识别。

## 产品原则

- MVP 阶段保留固定 6 秒 ASR 积木。
- 每个积木只标记声纹滑窗投票得到的主导发言人。
- 实时阶段允许显示临时结果，后台确认换人后再修正段落。
- 不确定时显示“未知发言人”，避免错误显示真实姓名。
- 自动识别结果必须允许用户重命名、合并和纠正。
- 声纹属于敏感生物特征，注册、保存和删除需要明确提示。

## 总体处理流程

```text
连续音频
  -> 统一重采样与预处理
  -> ASR 6 秒积木实时转写
  -> VAD 检测有效语音
  -> 声纹滑动窗口提取 Embedding
  -> 会议内说话人聚类
  -> 为完整积木标记主导 speaker_id
  -> 会议结束后全局聚类与结果修正
```

## 第一阶段：技术验证

- [x] 选择 sherpa-onnx 支持的 Speaker Embedding 模型。
- [x] 将模型文件放入 `backend/models/`，确保模型继续被 Git 忽略。
- [x] 在 `.env.example` 增加声纹模型路径与算法参数。
- [x] 实现注册音频与识别音频共用的预处理函数。
- [x] 音频进入后台时只重采样一次，统一为 `16kHz`、单声道、`float32`。
- [ ] 使用 VAD 去除静音，并跳过过短、削波严重、低信噪比窗口。
- [x] 使用受限 RMS 音量归一化，避免将背景噪声过度放大。
- [x] 对 Speaker Embedding 进行 L2 归一化。
- [x] 编写声纹相似度验证脚本，测试同一人和不同人的余弦相似度分布。
- [x] 根据真实样本确定初始相似度阈值，而不是直接写死经验值。

建议初始配置：

```env
HUIYI_SPEAKER_ENABLED=false
HUIYI_SPEAKER_EMBEDDING_MODEL=models/speaker-embedding.onnx
HUIYI_SPEAKER_WINDOW_SECONDS=1.5
HUIYI_SPEAKER_WINDOW_STEP_SECONDS=0.75
HUIYI_SPEAKER_SIMILARITY_THRESHOLD=0.55
HUIYI_SPEAKER_MIN_SEGMENT_SECONDS=1.0
HUIYI_SPEAKER_TARGET_RMS_DBFS=-22
HUIYI_SPEAKER_MAX_GAIN_DB=12
```

## 第二阶段：会议内说话人分离

- [x] ASR 与声纹分析共享统一的音频片段。
- [x] 使用约 `1.5 秒` 窗口、`0.75 秒` 步长提取 Embedding。
- [x] 计算滑动声纹窗口相似度，并投票选择积木主导发言人。
- [x] 实现会议内在线聚类，生成 `speaker_01`、`speaker_02` 等临时身份。
- [x] 6 秒积木只有一个说话人时，直接为现有转写段落标记说话人。
- [ ] MVP 暂不处理积木内多人换人和自动拆段。
- [ ] 会议结束后执行全局聚类，合并或修正实时阶段结果。
- [ ] 多人同时说话或置信度不足时标记为“发言人不确定”。

## 第三阶段：数据结构与接口

转写段落建议增加：

```json
{
  "speaker_id": "speaker_02",
  "speaker_name": null,
  "speaker_confidence": 0.86,
  "speaker_status": "identified"
}
```

- [x] 扩展 MongoDB 会议段落结构，兼容没有声纹字段的历史数据。
- [ ] 保存最终说话人时间区间和聚类中心。
- [x] 实时声纹窗口与 Embedding 默认只保存在内存中，避免数据库快速膨胀。
- [ ] 增加会议发言人列表接口。
- [ ] 增加发言人重命名接口。
- [ ] 增加合并两个会议内发言人的接口。
- [ ] 增加将单个段落重新指定给其他发言人的接口。
- [ ] 所有修改接口同步更新对应会议段落。

## 第四阶段：会议内前端管理

- [x] 转写段落显示发言人名称，并用稳定颜色区分不同说话人。
- [x] 约 `2.25 秒`后提前预判已注册身份，并在实时预览中显示。
- [ ] 历史会议增加“发言人”标签页。
- [ ] 展示检测到的发言人数、片段数和低置信度提示。
- [ ] 支持试听某位发言人的代表性音频。
- [ ] 支持将“发言人 1”重命名为真实姓名。
- [ ] 支持合并被错误拆分的发言人。
- [ ] 支持修正单个错误归属的段落。
- [ ] 人工修正后同步更新会议原文、AI 纪要来源与 Word 导出。

## 第五阶段：长期实名声纹库

- [x] 增加独立的“声纹库”页面。
- [x] 支持录制清晰语音注册身份。
- [ ] 支持从已确认的历史会议片段追加注册样本。
- [x] 每个身份保存多个样本 Embedding 和一个聚类中心。
- [ ] 注册样本覆盖不同时间、距离和常用麦克风。
- [ ] 识别时综合中心相似度、最佳样本相似度和当前音频质量。
- [x] 高置信度自动实名，低置信度保持未知。
- [x] 支持试听、追加样本和删除声纹。
- [x] 删除声纹资料时不删除历史会议和转写内容。
- [x] 明确展示声纹用途与删除影响。

## 性能策略

- [ ] 音频只在进入后台时重采样一次，ASR、VAD 与声纹共享标准音频。
- [ ] 无人声或质量不合格的窗口不执行 Embedding 推理。
- [ ] 默认使用 `0.75～1 秒` 步长，确认性能充足后再考虑缩短至 `0.5 秒`。
- [ ] 声纹提取与聚类放入独立后台任务，避免阻塞 WebSocket 音频接收。
- [ ] 限制每场会议保留的内存窗口数量，及时释放原始中间结果。
- [ ] 记录声纹推理耗时、队列积压和实时修正延迟。
- [ ] 在纯 CPU 环境下进行长时间会议压力测试。

## 测试与验收

- [ ] 同一人连续说话时不会频繁切换发言人。
- [ ] MVP 暂不要求处理两个人无静音衔接。
- [ ] MVP 暂不要求拆分一个积木内的多个发言人。
- [ ] 短停顿、咳嗽和背景噪声不会被识别成新发言人。
- [ ] 相同发言人在会议后续出现时能够归入同一身份。
- [ ] 会议结束后的全局修正不会丢失文字或破坏时间轴。
- [ ] 前端人工重命名、合并和段落纠正结果能够持久化。
- [ ] Word 导出能够展示最终发言人名称。
- [x] 开启声纹后，WebSocket 接收与实时转写基础回归测试通过。

## 明日建议实施顺序

1. 下载并验证 Speaker Embedding 模型。
2. 实现统一音频预处理和 Embedding 提取服务。
3. 使用本地样本统计同人、异人相似度。
4. 实现 6 秒积木内部的滑窗声纹分析与换人时间检测。
5. 先在后端日志和测试接口中展示检测结果，再接入实时转写段落。
